type ImageValidatorPropType = {
    file: File,
    minWidth?: number,
    minHeight?: number,
    maxWidth?: number,
    maxHeight?: number,
    isSquare?: boolean
};

export default ImageValidatorPropType;